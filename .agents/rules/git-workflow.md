# Git Workflow Rule

After completing any change, task, or modification requested by the user:
1. Verify tests and build pass cleanly.
2. Stage and commit all changes with a clear, structured conventional commit message.
3. Push to `origin main` immediately.
